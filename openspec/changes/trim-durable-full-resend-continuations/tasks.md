# Tasks

- [x] 1. Add durable bridge session columns and repository/coordinator plumbing for latest input count and fingerprint.
- [x] 2. Update HTTP bridge full-resend recovery to inject durable anchors only when stored input prefix metadata matches.
- [x] 3. Add regression coverage for trimmable durable full-resend and metadata persistence.
- [x] 4. Run targeted tests and spec validation.
