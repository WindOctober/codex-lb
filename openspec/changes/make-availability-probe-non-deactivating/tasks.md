# Tasks

- [x] Add a non-mutating token-refresh probe mode.
- [x] Wire manual account availability checks to non-mutating refresh.
- [x] Preserve normal deactivation behavior for production refresh paths.
- [x] Add focused unit coverage.
- [ ] Run `openspec validate --specs` (blocked locally: `openspec` CLI is not installed).
