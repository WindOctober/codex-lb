# Tasks

- [x] Make non-Codex provider compact fail explicitly instead of using streamed Responses surrogate fallback.
- [x] Keep provider unsupported-compact errors account-neutral.
- [x] Add focused regression coverage for provider compact fail-closed behavior.
- [ ] Run OpenSpec validation (blocked locally: `openspec` command is not installed).
