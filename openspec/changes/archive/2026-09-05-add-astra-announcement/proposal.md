# Astra introduction announcement

## Why
Astra is selectable, but the bootstrap catalog and the explicit API-key client catalog omit availability_nux. The installed extension therefore cannot show its existing Introducing GPT-6 Astra dialog.

## What changes
Supply the introduction metadata in the bootstrap entry and synchronize the operator client catalog. Preserve normal dismissal behavior and existing model defaults.

## Impact
Astra model discovery metadata and focused contract tests; no inference, pricing, authentication, or database changes.
